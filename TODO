[] Ask for hostname, username, and password for fetching the file ".bootie_secrets" over ssh (for setting up wireguard, github, etc)
[] Work on a way to send secrets back once authed
[] hostname that just redirects to https://raw.githubusercontent.com/MrAwesome/bootie/master/bs.sh
[] remote hosts should have a different PS1 color
[] add user to sudo/admin/wheel
